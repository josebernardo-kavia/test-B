"""Print a random integer between 1 and 1000 (inclusive) to stdout when executed.

Uses only Python's standard library and produces no additional output beyond the number.
"""

import random


if __name__ == "__main__":
    print(random.randint(1, 1000))
